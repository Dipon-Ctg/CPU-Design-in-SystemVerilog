1. include lower modules in the top module before instantiating 
2. muxes module: redundant mux definition?
3. instruction memory Size: 24 entries then the address needs only 5 bits, not 11 
4. instruction memory initialization ?? ..need some way to setup the array with some values at testbench (maybe add a write option and do it once)
5. 
